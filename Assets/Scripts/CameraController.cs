using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CameraController : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void FixedUpdate()
    {
        //  float x = 5 * Input.GetAxis ("Mouse X");
        //  float y = 5 * -Input.GetAxis ("Mouse Y");
         
        //  float cameraRotationX=Camera.main.transform.eulerAngles.x;
        //  float cameraRotationY=Camera.main.transform.eulerAngles.y;
        //  Camera.main.transform.rotation=Quaternion.Euler(cameraRotationX+y,cameraRotationY+x,0);
         //Camera.main.transform.Rotate (x, 0, y);
    }
}
